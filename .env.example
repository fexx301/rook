# Copy to .env for local development. Do not commit real secrets.

ROOK_DASHBOARD_TOKEN=change-me-to-a-long-random-token
ROOK_DATABASE_PATH=rook.db
ROOK_CONFIG=config.toml
RUST_LOG=info,rook=debug,tower_http=info
